// map() is method in array

// Map is object

let mycourse = ["AWS", "Azure", "GCP", "Alibaba"];

var mycloud = mycourse.map((mylearn) => {
  return "My Cloud Learning " + mylearn;
});

console.log(mycloud);
